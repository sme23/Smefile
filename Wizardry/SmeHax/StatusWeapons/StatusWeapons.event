//Status Weapons: Applies the status byte (status + duration) defined at item table entry + 0x22 if weapon has effect (+0x1F) 0xD.

#ifdef USE_STATUS_WEAPONS

  #include "StatusWeapons.lyn.event"

#endif // USE_STATUS_WEAPONS
